version: '3.7'
services:
  db:
    image: colemanword/thermomatic:latest
    env_file:
      - thermomatic.env
    ports:
      - "1337:1337"
      - "1338:1338"
    networks:
      default:
        aliases:
          - thermomatic
networks:
  default:

volumes:
  default:
